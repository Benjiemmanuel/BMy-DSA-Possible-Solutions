function whatIsRecursion() {
  // Recursion is a function that calls itself.
  /*If you find yourself breaking down a problem into smaller pieces of the same problem, then recursion is the solution.*/
  //e.g. A principal is asked to give the total number of students in the school. Here the principal can get the number of students from each teacher in each class and add them up.
}
